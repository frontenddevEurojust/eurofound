(function($) {
    $(document).ready(function(){
        // Manage chosen option via change function as it's the only one accepted by Chrome
        $('.form-item-path-alias .description').text('Field content should follow structure "topics/foo" to create the url alias "topic/foo".');
    });
})(jQuery)