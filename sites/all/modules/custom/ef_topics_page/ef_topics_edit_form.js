(function($) {
    $(document).ready(function(){
        // Manage chosen option via change function as it's the only one accepted by Chrome
        $('.form-item-path-alias .description').text('Introduced URL alias should follow the following structure: "topics/manual-url".');
    });
})(jQuery)